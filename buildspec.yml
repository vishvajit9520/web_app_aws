version: 0.2

phases:
  build:
    commands:
      - docker build -t ecs-app .
      - docker tag ecs-app:latest <account_id>.dkr.ecr.ap-south-1.amazonaws.com/ecs-app:latest
      - docker push <account_id>.dkr.ecr.ap-south-1.amazonaws.com/ecs-app:latest
